{
  "name": "interactive-cmd",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/runner.js --bundle --format=esm --platform=browser --target=es2022 --outfile=dist/runner.js",
    "test": "node scripts/smoke-test.mjs",
    "serve": "npx http-server . -p 4173 -c-1"
  },
  "dependencies": {
    "@biowasm/aioli": "^3.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "http-server": "^14.1.1",
    "playwright": "^1.54.0"
  }
}
